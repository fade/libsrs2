[DEFAULT]
debian-branch = main

[buildpackage]
sign-tags = False
export-dir = ../build-area
# libsrs2 has no upstream branch/tag and ships no orig tarball; binary-only
# builds never consume one, so skip orig-tarball generation entirely.
no-create-orig = True

[dch]
auto = True
